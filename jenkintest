pipeline {
    agent any

    stages {
        stage('install maven') {
            steps {
               sh 'sudo dnf install maven -y'
            }
        }
     stage('creation of maven package') {
            steps {
             sh 'mvn clean package'
            }
        }
     stage('releasing the maven package') {
            steps {
             archiveArtifacts artifacts: 'target/*.jar', followSymlinks: false
            }
        }
    }
}
